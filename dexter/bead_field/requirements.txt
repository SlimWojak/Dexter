# Bead Field Gate 1 — validated on ARM Mac Mini (Python 3.14.2)
# Phase 0 dependency validation: 2026-02-22

# Schema
pydantic>=2.12.0
uuid6>=2025.0.1

# Signing
ecdsa>=0.19.0
pqcrypto>=0.4.0

# Data
pyyaml>=6.0

# Testing
pytest>=9.0.0
pytest-cov>=7.0.0

# stdlib (no pip install needed): sqlite3, hashlib, json, datetime, threading
